{{- if .Values.pypiserver.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "pypiserver.fullname" . }}
  labels:
    {{- include "pypiserver.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "pypiserver.fullname" . }}
  minReplicas: {{ .Values.pypiserver.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.pypiserver.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.pypiserver.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.pypiserver.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.pypiserver.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.pypiserver.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
